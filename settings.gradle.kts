pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "Bolt Clone Compose"
include (":app")
include(":cache")
include(":core")
include(":domain")
include(":data")
